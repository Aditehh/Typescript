const User = {
    name: "hitesh",
    email: "hitesh@lco.dev",
    isactive: true
}

function createuser({ name: string, ispaid: boolean }) { }

createuser({ name: "aditua", ispaid: false })

function createcourse({ }): { name: string, price: number } {
    return { name: "reactjs", price: 990 }
}

export { }